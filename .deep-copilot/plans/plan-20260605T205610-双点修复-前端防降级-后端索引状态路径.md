# 双点修复：前端防降级 + 后端索引状态路径

> Generated by Deep Copilot · Plan mode
> 2026-06-05T12:56:10.551Z

## 目标 / Goal
双点修复：前端添加防降级守卫，后端 get_indexing_status 单点硬编码修正读取路径（不改变全局 DATA_ROOT）

## 方案 / Approach
最小化单点修复。前端一行 guard，后端在 get_indexing_status 函数内部用局部变量覆盖路径计算，零影响其他端点。

## 步骤 / Steps
1. **TocDrawer.tsx 添加防降级守卫** — 在 poll 成功回调中，在 setIndexingStatus 之前添加：const newStatus = s.status ?? 'pending'; if (indexingStatus === 'processing' && newStatus === 'pending') { console.warn(...); return; }
2. **resource_router.py get_indexing_status 硬编码正确路径** — 在函数内计算 real_data_root = Path(__file__).resolve().parent.parent.parent / 'data'，meta_file 改用 real_data_root / 'wiki' / book_name / '.meta.json'，不改变全局 WIKI_ROOT 或 DATA_ROOT
3. **编译验证** — 前端 npx tsc --noEmit + npx next build；后端 import 检查

## 涉及文件 / Affected files
- `app/app/components/nav/TocDrawer.tsx` — 添加防降级守卫（4 行）
- `backend/app/resource_router.py` — get_indexing_status 函数内硬编码 real_data_root 路径（不改全局变量）

## 风险与缓解 / Risks
- 前端防降级守卫依赖闭包中的 indexingStatus 值，由于 useEffect 依赖包含 indexingStatus，闭包始终为最新值，安全

## 下一步 / Next steps
- 切换到 Agent 模式执行以上 2 处修改 + 编译验证

