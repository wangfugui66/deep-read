# 回滚：撤销 round 2+3，保留 round 1

> Generated by Deep Copilot · Plan mode
> 2026-06-05T12:44:44.692Z

## 目标 / Goal
撤销第2轮（DATA_ROOT 修复 + 防降级守卫）和第3轮（集中化配置 + 前端守卫）的所有改动，保留第1轮的 console.log 埋点、轮询引擎重写、按钮互斥渲染。

## 方案 / Approach
逐文件精确定位需要替换的代码块：TocDrawer.tsx 移除防降级守卫；books/page.tsx 移除 Array.isArray；resource_router.py 恢复独立 DATA_ROOT 和原始 list_books；8 个 service 文件恢复各自的硬编码 DATA_ROOT；删除 core/config.py。

## 步骤 / Steps
1. **撤销 TocDrawer.tsx 防降级守卫** — 删除 polling callback 中的 4 行 guard（const newStatus = ... / if (...) / return; / }），恢复直接 setIndexingStatus(s.status ?? 'pending')
2. **撤销 books/page.tsx Array.isArray** — loadBooks() 中 .then((data) => setBooks(Array.isArray(data) ? data : [])) 改回 .then(setBooks)；grid 的 Array.isArray(books) && 条件去掉
3. **删除 core/config.py** — 删除 D:\DeepRead-v2\backend\app\core\config.py
4. **恢复 resource_router.py 原始状态** — 删除 from app.core.config import ...；恢复 DATA_ROOT = Path(__file__).resolve().parent.parent / 'data' + RAW_ROOT/WIKI_ROOT；删除 list_books 的 try/except 包裹
5. **恢复 8 个 service 文件的独立 DATA_ROOT** — indexer/chat_session/note/profile/quiz/skeleton_service.py + book_pipeline.py + rg_searcher.py：删除 config import，恢复各自原有的硬编码 DATA_ROOT 声明
6. **编译验证** — 前端 npx tsc --noEmit + npx next build；后端全量 import 检查

## 涉及文件 / Affected files
- `app/app/components/nav/TocDrawer.tsx` — 移除防降级守卫（round 2）
- `app/app/books/page.tsx` — 移除 Array.isArray 守卫（round 3）
- `backend/app/core/config.py` — 删除（round 3 新建）
- `backend/app/resource_router.py` — 恢复原始 DATA_ROOT + 删除 config import + 删除 try/except
- `backend/app/services/indexer_service.py` — 恢复原始 DATA_ROOT（4 parents）
- `backend/app/services/book_pipeline.py` — 恢复原始 _DATA_ROOT / RAW_ROOT / WIKI_ROOT（4 parents）
- `backend/app/services/chat_session_service.py` — 恢复原始 _DATA_ROOT（3 parents）
- `backend/app/services/note_service.py` — 恢复原始 Path(__file__).parent.parent / 'data'
- `backend/app/services/profile_service.py` — 恢复原始 DATA_ROOT（3 parents）
- `backend/app/services/quiz_service.py` — 恢复原始 DATA_ROOT（3 parents）
- `backend/app/services/rg_searcher.py` — 恢复原始 _DATA_ROOT（3 parents）
- `backend/app/services/skeleton_service.py` — 恢复原始 DATA_ROOT（3 parents）

## 风险与缓解 / Risks
- 各 service 文件的独立 DATA_ROOT 路径计算不一致（3/4/2 parents），回滚后路径错位问题会重现（resource_router 指向 backend/data，indexer 指向 DeepRead-v2/data），这是第2轮修复的原始问题
- TocDrawer.tsx 的 console.log 和轮询重写属于 round 1 改动，不会被回滚
- 无 git 历史，回滚后无法 diff 验证，需手动对比

## 下一步 / Next steps
- 切换到 Agent 模式
- Agent 将按上述 5 个步骤执行回滚
- 编译验证通过后确认

## 备注 / Notes
回滚后各文件各自的 DATA_ROOT 会再次不一致（这是第2轮之前的状态），但这是用户明确要求的。
