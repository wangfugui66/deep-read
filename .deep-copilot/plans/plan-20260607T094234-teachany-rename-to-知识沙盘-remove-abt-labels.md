# TeachAny: rename to 知识沙盘 + remove ABT labels

> Generated by Deep Copilot · Plan mode
> 2026-06-07T01:42:34.943Z

## 目标 / Goal
Fix two TeachAny issues in a single file (teachany_service.py): (1) rename "互动课件" → "知识沙盘" in the user prompt, (2) rewrite the ABT prompt to prevent mechanical "And/But/Therefore" labels from appearing in generated HTML.

## 方案 / Approach
Both fixes are in `backend/app/services/teachany_service.py`. Task 1 is a single user-message string replacement. Task 2 rewrites rule #4 in `TEACHANY_SYSTEM_PROMPT` to replace the ABT framework instruction with a natural "背景-冲突-结论" narrative requirement plus strict anti-label warnings. Also adds a title-formatting instruction so the LLM uses "知识沙盘" in the HTML `<title>` tag.

## 步骤 / Steps
1. **Task 1: 互动课件 → 知识沙盘 (user prompt)** — Change the user message from `生成互动课件` to `生成知识沙盘`
2. **Task 2: rewrite ABT → natural narrative prompt** — Rewrite rule #4 in TEACHANY_SYSTEM_PROMPT: replace ABT with natural 背景-冲突-结论 narrative, add absolute ban on mechanical labels (And/But/Therefore/安/但/因此), add `<title>` format instruction using 知识沙盘
3. **Verify Python syntax** — Run `python -m py_compile backend/app/services/teachany_service.py` to verify syntax
4. **Commit and push** — Commit with message 'TeachAny: rename 互动课件→知识沙盘 + remove ABT mechanical labels'

## 涉及文件 / Affected files
- `backend/app/services/teachany_service.py` — Sole file containing both the system prompt (ABT instruction + title guidance) and the user message (互动课件 string)

## 风险与缓解 / Risks
- Cache invalidation: existing cached HTML files will still contain old wording until regenerated — acceptable since cache is runtime-generated and not in Git
- LLM behavior change: the rewritten prompt may produce slightly different HTML structure; acceptable risk given the anti-hallucination benefit

## 下一步 / Next steps
- Switch to Agent mode to execute the 2 replacements
- Verify Python syntax with `python -m py_compile`
- Commit and push

