# TeachAny: rename to 知识沙盘 + remove ABT labels (v2)

> Generated by Deep Copilot · Plan mode
> 2026-06-07T01:46:36.994Z

## 目标 / Goal
Fix two TeachAny issues in teachany_service.py: (1) replace "互动课件" → "知识沙盘" in both the LLM user prompt AND via a new Python post-processing sanitation step, (2) rewrite the ABT prompt to prevent mechanical labels, with positive "背景-冲突-结论" fusion guidance.

## 方案 / Approach
双保险策略：（1）修改用户提示词，告知 LLM 生成'知识沙盘'而非'互动课件'；（2）在 generate_courseware() 现有 markdown 栅栏移除之后、缓存写入之前，插入一个新的 `_sanitize_html()` 后处理步骤，对所有 HTML 文件执行 str.replace('互动课件', '知识沙盘')。同时，针对 ABT 问题重写系统提示词中的第 4 条规则。仅修改单个文件：backend/app/services/teachany_service.py。

## 步骤 / Steps
1. **Task 1: 双保险 — Prompt + Python 后处理替换 互动课件→知识沙盘** — 发现：LLM 生成完整 HTML，无 Python 模板组装。但我将添加一个后处理函数 `_sanitize_html()` 在所有缓存写入前对所有 HTML 执行 str.replace('互动课件', '知识沙盘')，确保即使 Prompt 被 LLM 忽略，最终输出也不含旧词汇。同时修改用户消息：'生成互动课件' → '生成知识沙盘'。
2. **Task 2: 重写 ABT → 背景-冲突-结论 自然叙事** — 替换 TEACHANY_SYSTEM_PROMPT 中的 Rule #4：将 'ABT narrative: And-But-Therefore' 替换为自然中文叙事要求，附加强制禁止列表 (And/But/Therefore/安/但/因此) 及正面引导（'无缝融合成一段连贯、优美的中文自然段落，使用自然的语义关联词进行过渡'）。
3. **Python 语法验证** — 运行 python -m py_compile backend/app/services/teachany_service.py 验证语法。
4. **备注：缓存清理提示（见 Next Steps）** — 提醒用户：测试前需清空 data/plugins/teachany_cache/ 下的旧 HTML 缓存，以触发全新生成。
5. **提交并推送** — 提交信息：'TeachAny: rename 互动课件→知识沙盘 (prompt + post-process) + remove ABT labels'

## 涉及文件 / Affected files
- `backend/app/services/teachany_service.py` — 用户消息修改、Prompt 重写、新增后处理 sanitizer——全部在同一文件中完成

## 风险与缓解 / Risks
- 现有缓存 HTML 文件仍含旧措辞，直至重新生成——部署说明中将提醒用户清空缓存
- Prompt 重写后 LLM 行为变化——可接受；反幻觉收益高于风险
- str.replace('互动课件', '知识沙盘') 可能遗漏边缘大小写变体——但常规 LLM 输出一致性好，风险极低

## 下一步 / Next steps
- 切换到 Agent 模式执行
- 测试前：清空 data/plugins/teachany_cache/ 下旧缓存以触发新生成
- 向用户汇报修改了哪些具体行

## 备注 / Notes
代码审查发现：与最初假设相反，目前不存在 Python 级别的 HTML 模板组装。LLM 通过 FileResponse 直接生成并返回完整 HTML。因此，后处理 sanitizer 是我们为实现“UI 模板归代码”目标所能添加的最接近等价方案——它确保在 Python 层面进行清洗后再存入缓存。
